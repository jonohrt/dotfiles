import Uri from 'vscode-uri';
import { TypeScriptVersion } from './versionProvider';
export default class VersionStatus {
    private readonly _normalizePath;
    private readonly enableJavascript;
    private readonly _onChangeEditorSub;
    private readonly _versionBarEntry;
    constructor(_normalizePath: (resource: Uri) => string | null, enableJavascript: boolean);
    dispose(): void;
    onDidChangeTypeScriptVersion(version: TypeScriptVersion): void;
    private showHideStatus;
}
