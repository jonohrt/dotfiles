import { TaskOptions } from 'coc.nvim';
import { CommandManager } from 'coc.nvim/lib/commands';
import { Disposable } from 'vscode-languageserver-protocol';
export default class WatchProject implements Disposable {
    private disposables;
    static readonly id: string;
    static readonly startTexts: string[];
    private statusItem;
    private task;
    private options;
    constructor(commandManager: CommandManager);
    private check;
    private start;
    private onStop;
    private onStart;
    private onLine;
    getOptions(): TaskOptions;
    dispose(): void;
}
